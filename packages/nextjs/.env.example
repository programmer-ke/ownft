# The following need to be setup
PINATA_JWT=
NEXT_PUBLIC_PINATA_GATEWAY=

# If the variable below are not set, the default ones will be used (see scaffold.config.ts)
# For production, it is recommended to set them
NEXT_PUBLIC_ALCHEMY_API_KEY=
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=
